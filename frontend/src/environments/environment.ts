// Questo è il file di configurazione dell'ambiente.
// Contiene le impostazioni per l'ambiente di sviluppo.

// Definizione dell'oggetto environment che esportiamo.
export const environment = {
  // Impostazione di produzione su false durante lo sviluppo.
  production: false,
  // Definizione dell'URL dell'API utilizzato durante lo sviluppo locale.
  apiUrl: 'http://127.0.0.1:8000/api', // Modifica questo URL se necessario per l'ambiente di sviluppo.
};
